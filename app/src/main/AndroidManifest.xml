<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmh.itics.tesoem.edu.p4gmh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ardilla2Activity"></activity>
        <activity android:name=".tiburon2Activity" />
        <activity android:name=".cobraActivity" />
        <activity android:name=".pericoActivity" />
        <activity android:name=".gatoActivity" />
        <activity android:name=".osoActivity" />
        <activity android:name=".loboActivity" />
        <activity android:name=".perroActivity" />
        <activity android:name=".frmmenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>